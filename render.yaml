services:
  - type: web
    name: opusflow-backend
    env: node
    rootDir: backend
    buildCommand: npm install && npx prisma generate
    startCommand: node scripts/create-db-components.js && npm start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: opusflow-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_REFRESH_SECRET
        generateValue: true
      - key: NODE_ENV
        value: production

databases:
  - name: opusflow-db
    databaseName: opusflow
    user: postgres
    plan: free
